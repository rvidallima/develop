for (let x = 1; x<=40 ; x++) {
    let tipo = x % 2 == 0 ? 'par' : 'impar'
    console.log(x, tipo)
}


for (let x = 40; x>=1 ; x--) {
    let tipo = x % 2 == 0 ? 'par' : 'impar'
    console.log(x, tipo)
}